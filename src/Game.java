public class Game {
    
    /* 
    public void launch(){
        init();
        long previousTimer = Systemn.currentTimeMillis();
        while(isGameRunning()){
            long currentTime = System.currentTimeMillis();
            double deltaTimeSec = (double)(currentTime - previousTime)/1000;
            previousTime = currentTime;
            update(deltaTimeSec);
        }
    }
    private boolean isGameRunning(){
        if(//player.pdv == 0){
            //defaite
            return false;
        }
        if else (//AllEnnemies.dead==true && vague3.end==true){
            return true;
        }
    }
    */
   
}
